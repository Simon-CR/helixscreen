diff --git a/src/core/lv_obj.h b/src/core/lv_obj.h
--- a/src/core/lv_obj.h
+++ b/src/core/lv_obj.h
@@ -35,6 +35,9 @@
  *      DEFINES
  *********************/
 
+/*lv_obj_add_flag/lv_obj_remove_flag tolerate a parentless object (a screen)*/
+#define HELIX_LV_OBJ_FLAG_SCREEN_PARENT_GUARD 1
+
 /**********************
  *      TYPEDEFS
  **********************/
diff --git a/src/core/lv_obj.c b/src/core/lv_obj.c
--- a/src/core/lv_obj.c
+++ b/src/core/lv_obj.c
@@ -277,7 +277,7 @@
     }
 
     if((was_on_layout != lv_obj_is_layout_positioned(obj)) || (f & (LV_OBJ_FLAG_LAYOUT_1 |  LV_OBJ_FLAG_LAYOUT_2))) {
-        lv_obj_mark_layout_as_dirty(lv_obj_get_parent(obj));
+        if(lv_obj_get_parent(obj)) lv_obj_mark_layout_as_dirty(lv_obj_get_parent(obj));
         lv_obj_mark_layout_as_dirty(obj);
     }
 
@@ -307,12 +307,14 @@
 
     if(f & LV_OBJ_FLAG_HIDDEN) {
         lv_obj_invalidate(obj);
-        lv_obj_mark_layout_as_dirty(lv_obj_get_parent(obj));
+        /*A screen has no parent: unhiding one must not dereference NULL*/
+        lv_obj_t * parent = lv_obj_get_parent(obj);
+        if(parent) lv_obj_mark_layout_as_dirty(parent);
         lv_obj_mark_layout_as_dirty(obj);
     }
 
     if((was_on_layout != lv_obj_is_layout_positioned(obj)) || (f & (LV_OBJ_FLAG_LAYOUT_1 |  LV_OBJ_FLAG_LAYOUT_2))) {
-        lv_obj_mark_layout_as_dirty(lv_obj_get_parent(obj));
+        if(lv_obj_get_parent(obj)) lv_obj_mark_layout_as_dirty(lv_obj_get_parent(obj));
     }
 
 }
